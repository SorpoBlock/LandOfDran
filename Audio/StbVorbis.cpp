//stb_vorbis's implementation, kept away from the project's headers since it has its own static error() that clashes with Logger's
//SoundFile.cpp includes it with STB_VORBIS_HEADER_ONLY for just the declarations
#include "../External/stb_vorbis.c"
